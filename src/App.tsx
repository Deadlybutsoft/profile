import { Scene } from "@/components/Scene";

export default function App() {
  return (
    <div className="fixed inset-0 w-screen h-[100dvh] overflow-hidden bg-black">
      <Scene />
    </div>
  );
}
